# NanoLoc 功能待办

## 已确认待设计 / 待实现

### 项目术语表

记录日期：2026-06-11

目标分支：0.4.0

来源：用户提出新阶段需求。当前人工维护的术语表字段包括：类别、场景、中文、英文、说明、其他语言参考。

初步判断：

- 不建议直接照搬“中文 / 英文 / 其他语言参考”的固定表格结构。
- NanoLoc 的项目语言是可配置的，每个项目都有自己的基础语言和目标语言；术语表也应跟随项目语言配置，而不是固定为中英文。
- 更合适的设计是“术语概念 + 多语言术语值”：一条术语记录描述一个概念，再为项目已配置语言维护推荐译法、参考译法和注意事项。
- 术语表应服务于翻译流程，而不只是一个独立表格：人工校对能查，AI 翻译能用，后续还能做术语一致性检查。

推荐数据模型：

- `GlossaryEntry`
  - `id`
  - `projectId`
  - `category`：类别，例如 UI、支付、账号、法律、营销。
  - `scenario`：场景，例如首页、登录页、订阅弹窗、错误提示。
  - `sourceText`：术语概念的主表达，建议使用项目基础语言；如果业务方更习惯中文，也可由配置决定。
  - `description`：说明，解释术语含义、使用边界、语气要求。
  - `notes`：内部备注，可放人工维护说明。
  - `sortOrder`
  - `createdAt` / `updatedAt`
- `GlossaryTermValue`
  - `id`
  - `glossaryEntryId`
  - `languageCode`
  - `term`：该语言推荐使用的术语。
  - `aliases`：可接受的别名或参考写法，建议用 JSON array 保存。
  - `forbiddenTerms`：不建议或禁止使用的译法，建议用 JSON array 保存。
  - `reference`：其他语言参考或补充说明；原人工表里的“其他语言参考”可以落到这里。
  - `updatedAt`

为什么这样设计：

- “中文 / 英文”不应成为数据库固定字段，否则项目基础语言不是中文或英文时会很别扭。
- 多语言术语值可以自然支持当前项目的 `baseLanguage + targetLanguages`。
- 后续如果项目增加语言，只需要为新语言补 `GlossaryTermValue`。
- `aliases` 和 `forbiddenTerms` 能支撑后续术语一致性检查，而原表格只有“参考”不够约束翻译结果。

建议产品入口：

- 项目详情页增加“术语表”入口，建议放在项目工具栏或项目设置附近。
- 术语表页面支持搜索、按类别筛选、按场景筛选。
- 术语表行内展示：类别、场景、基础语言术语、目标语言术语完成度、说明。
- 点击术语进入编辑弹窗或详情页，按项目语言显示每种语言的推荐术语、别名、禁用词、参考说明。

如何嵌入当前翻译流程：

1. 人工翻译 / 校对阶段
   - 在词条行或翻译单元格附近显示命中的术语提示。
   - 命中规则可以先做简单包含匹配：源文、Key、Remarks 中包含术语主表达时，提示相关术语。
   - 提示内容应包含：类别、场景、推荐译法、禁用译法、说明。

2. AI 单格翻译
   - 翻译前根据当前词条的 Key、Remarks、基础语言内容，找出相关术语。
   - 只把命中的术语注入 AI prompt，不要把整个术语表都发给模型，避免 token 膨胀和误导。
   - prompt 中明确要求：优先使用目标语言推荐术语，避免 forbidden terms。

3. AI 整列 / 批量翻译
   - 每个 batch 翻译前，按 batch 中所有源文本汇总命中术语。
   - 控制注入数量，例如最多 20-50 条命中术语，按匹配强度或出现次数排序。
   - 如果没有命中术语，不改变现有翻译流程。

4. 术语一致性检查
   - 后续可新增“术语检查”按钮。
   - 检查项目翻译内容是否没有使用推荐术语，或使用了 forbidden terms。
   - 结果可以按语言、术语、词条分组展示。

5. 导入 / 导出
   - 术语表应有独立 CSV 导入 / 导出，不混入项目翻译 CSV。
   - 建议 CSV 模板列为：`Category`, `Scenario`, `Source Term`, `Description`, `Language`, `Term`, `Aliases`, `Forbidden Terms`, `Reference`。
   - 如果用户坚持人工表格风格，也可提供宽表导入模板：`Category`, `Scenario`, `Description`, `en-US`, `zh-CN`, `pt-BR` 等项目语言列。

建议分阶段实现：

- 0.4.0 MVP
  - 数据库 schema：新增项目级术语表模型。
  - 项目术语表页面：列表、搜索、类别/场景筛选、创建、编辑、删除。
  - 术语表 CSV 导入 / 导出。
  - 在 AI 单格翻译和批量翻译 prompt 中注入命中术语。
  - 最小验证脚本或单元测试覆盖术语匹配和 prompt 注入。
- 0.4.x 增强
  - 表格翻译单元格旁显示术语提示。
  - 术语一致性检查报告。
  - forbidden terms 检测。
  - 术语命中高亮。
  - 从现有翻译内容中半自动提取候选术语。

初步技术影响范围：

- Prisma schema：新增 `GlossaryEntry` / `GlossaryTermValue`，并关联 `Project`。
- 项目详情页：增加术语表入口或 tab。
- 新 API / server actions：术语 CRUD、导入、导出、搜索。
- AI 翻译服务：在单格、整列、批量翻译前查询相关术语并注入 prompt。
- i18n：新增术语表相关中英文 UI 文案。
- 验证：增加术语匹配和 CSV import/export 的测试或脚本。

需要确认的问题：

- 术语主表达以哪种语言为准？使用项目基础语言，还是固定中文更符合你们团队维护习惯？
- 一个术语在同一语言里是否允许多个推荐译法？还是只能一个推荐词，其他都算参考或别名？
- 是否需要“禁用译法 / forbidden terms”？如果需要，AI prompt 和术语检查都会用到。
- 术语表是严格项目级，还是未来需要跨项目复用或从一个项目复制到另一个项目？
- 术语命中时，是只提示人工，还是保存翻译时也要警告甚至阻止不符合术语的内容？
- 术语表 CSV 导入更希望使用“长表格式”（一行一个语言术语值），还是“宽表格式”（一行一个术语，多列语言）？
- Pull API 或翻译 CSV 是否需要包含术语表？当前建议不包含，单独提供术语表导入 / 导出。

## 已实现

### 二阶段交付前扫尾

记录日期：2026-06-11

实现分支：0.3.26

需求摘要：

- 清理二阶段审计中发现的低频 CRUD 查询遗留。
- 统一项目词条 API、项目页搜索和搜索结果导出的语言范围语义。
- 降低搜索结果导出按钮带来的重复查询成本。
- 补充可重复执行的最小格式契约验证脚本，辅助人工验收导入、导出和 Pull API 格式一致性。

实现状态：

- 已将词条创建、更新、清空翻译后的返回查询从 `include: { values: true }` 收口为明确 `select`。
- 已让 `/api/projects/[id]/terms` 按项目已配置语言搜索和返回值。
- 已让项目详情页复用同一次列表查询的 total 驱动分页和搜索结果导出按钮禁用态。
- 已新增 `verify:contracts` 脚本，验证 Android XML、H5 JSON、iOS strings 解析，以及 CSV/Pull JSON/Pull XML 输出格式契约。

### 搜索结果导出 CSV

记录日期：2026-06-11

实现分支：0.3.25

来源：同事需求，经用户反馈记录。

需求摘要：

- 当项目详情页表格存在搜索条件时，提供“仅导出搜索结果”的 CSV 导出能力。
- 用户倾向将“导出搜索结果”按钮放在搜索输入框旁边。

初步评估：

- 当前表格搜索由 URL 参数 `search` 驱动。
- 当前表格搜索会匹配键名、备注、以及翻译内容。
- 预设可选语言数量是固定的，但每个项目只启用自己选择的语言；搜索结果导出只应在该项目已选择/配置的语言集合内工作，不涉及未被项目选择的预设语言。
- 当前 CSV 导出不读取搜索条件，会导出整个项目，并保留 UTF-8 BOM、`Remarks` 列、基础语言和全部目标语言列。
- 建议在保留当前“导出全量 CSV”按钮的同时，扩展现有流式 CSV 导出路径，支持可选 `search` 参数。

已确认行为：

- 在搜索输入框旁增加一个次级按钮。
- 没有搜索条件或没有搜索结果时，按钮禁用。
- 导出命中搜索条件的全部行，不只导出当前分页。
- 导出的列与当前整个项目 CSV 导出保持一致：`Key`、`Remarks`、基础语言、项目已选择的全部目标语言。
- CSV 不包含截图。
- 文件名可以包含搜索词，例如 `Project_search_<term>.csv`。
- 保持现有 CSV escaping、UTF-8 BOM 和流式响应行为。

建议实现方向：

- 在项目详情页 `SearchFilter` 附近增加一个小型 client 导出按钮。
- 将当前 `search` 参数传给 `/api/projects/[id]/export`。
- 扩展 `createCsvExportStream` / CSV chunk generation，支持可选搜索过滤，并在项目已选择语言范围内匹配键名、备注和翻译内容。
- 抽出与 `listTerms` 共用或等价的搜索 where-clause 逻辑，确保表格搜索和导出搜索语义一致。
- 补充新按钮 label、tooltip、禁用状态等 i18n 文案。

实现状态：

- 已扩展 `/api/projects/[id]/export`，支持可选 `search` 参数。
- 已保持 CSV 列、UTF-8 BOM、escaping、流式响应与全量导出一致。
- 已在项目详情页搜索框旁增加“导出搜索结果”按钮。
- 已在无搜索条件或无搜索结果时禁用按钮。
- 已将表格搜索和搜索结果导出统一为项目已配置语言范围内匹配。
