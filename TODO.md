# NanoLoc 功能待办

## 待确认

### 搜索结果导出 CSV

记录日期：2026-06-11

来源：同事需求，经用户反馈记录。

需求摘要：

- 当项目详情页表格存在搜索条件时，提供“仅导出搜索结果”的 CSV 导出能力。
- 用户倾向将“导出搜索结果”按钮放在搜索输入框旁边。

初步评估：

- 当前表格搜索由 URL 参数 `search` 驱动。
- 当前表格搜索会匹配键名、备注、以及翻译内容。
- 出于性能考虑，当前项目详情页搜索只传入基础语言和当前可见目标语言列。
- 当前 CSV 导出不读取搜索条件，会导出整个项目，并保留 UTF-8 BOM、`Remarks` 列、基础语言和全部目标语言列。
- 建议在保留当前“导出全量 CSV”按钮的同时，扩展现有流式 CSV 导出路径，支持可选 `search` 参数。

建议默认行为：

- 在搜索输入框旁增加一个次级按钮，仅在 `search` 非空时显示或启用。
- 导出命中搜索条件的全部行，不只导出当前分页。
- 导出的列建议与当前全量 CSV 保持一致：`Key`、`Remarks`、基础语言、全部目标语言。
- CSV 不包含截图。
- 保持现有 CSV escaping、UTF-8 BOM 和流式响应行为。

待确认问题：

- “搜索结果”是否必须严格等同当前表格结果，包括当前可见语言列过滤；还是导出时应搜索项目内全部语言？
- 导出的 CSV 应包含全部语言列，还是只包含当前可见语言列？
- 没有搜索条件时，这个按钮应该隐藏、禁用，还是退化为全量导出？
- 导出范围是否确认是所有命中行，而不是当前分页？
- 文件名是否需要包含搜索词，例如 `Project_search_<term>.csv`，还是沿用当前项目导出文件名模式？

建议实现方向：

- 在项目详情页 `SearchFilter` 附近增加一个小型 client 导出按钮。
- 将当前 `search` 参数传给 `/api/projects/[id]/export`。
- 扩展 `createCsvExportStream` / CSV chunk generation，支持可选搜索过滤。
- 抽出与 `listTerms` 共用的搜索 where-clause 逻辑，确保表格搜索和导出语义一致。
- 补充新按钮 label、tooltip、禁用状态等 i18n 文案。
