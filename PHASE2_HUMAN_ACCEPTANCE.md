# NanoLoc 0.3.26 人工验收测试说明

## 1. 当前结论

`TODO.md` 当前没有待实现功能，只有已实现记录。

以下内容已经由 agent 在 `0.3.26` 分支完成验证，不需要人工重复作为验收项：

| 已验证内容 | 验证方式 |
| --- | --- |
| TypeScript 类型检查 | `./node_modules/.bin/tsc --noEmit` |
| ESLint | `./node_modules/.bin/eslint .` |
| 生产构建 | `./node_modules/.bin/next build --webpack` |
| whitespace 检查 | `git diff --check` |
| 导入/导出/Pull API 格式契约 | `npm run verify:contracts` |
| CSV BOM、CSV escaping、Pull JSON/XML formatter | 自动脚本覆盖 |
| 低频 CRUD 的 `include: { values: true }` 遗留 | 代码扫描确认已清理 |

这份文档只保留真正需要人工验收的内容：浏览器交互、真实文件上传下载、Excel/Numbers 打开效果、部署持久化、外部 AI 服务、以及一二阶段改动可能影响的核心业务路径。

## 2. 验收准备

### 2.1 确认测试分支

```bash
git status --short --branch
```

期望当前分支是 `0.3.26`。

### 2.2 启动应用

确保 `.env` 至少包含：

```env
DATABASE_URL="file:./dev.db?journal_mode=WAL&busy_timeout=5000"
AUTH_SECRET="replace-with-a-secret"
API_ACCESS_TOKEN="replace-with-a-test-token"
```

启动：

```bash
npx prisma generate
npx prisma db push
npm run dev
```

打开：

```text
http://localhost:3000
```

### 2.3 准备测试项目和基础数据

建议新建项目，避免旧数据干扰。

项目配置：

| 字段 | 值 |
| --- | --- |
| 项目名 | `Phase 2 Acceptance` |
| 基础语言 | `en-US` |
| 目标语言 | `zh-CN`, `pt-BR`, `ja-JP` |

建议新建这些词条：

| Key | Remarks | en-US | zh-CN | pt-BR | ja-JP |
| --- | --- | --- | --- | --- | --- |
| `home.title` | `Home screen title` | `Hello, "world"` | `你好` | `Olá, ação` | `こんにちは` |
| `home.subtitle` | `Contains comma and newline` | `Line 1, welcome` | `第一行` | `Linha 1` | `行 1` |
| `button.save` | `CTA button` | `Save` | `保存` | `Salvar` | `保存` |
| `button.save.copy` | `Duplicate zh-CN value` | `Save copy` | `保存` | `Salvar cópia` | `保存コピー` |

这一步本身也属于验收：确认项目创建、语言配置、词条创建在浏览器里没有异常。

## 3. 必测项一：项目详情页核心交互回归

原因：`0.3.26` 重组了项目详情页的数据流，复用同一次列表查询结果驱动表格、分页和“导出搜索结果”按钮。自动检查能保证代码编译，但不能保证浏览器交互体验。

测试步骤：

1. 打开 `Phase 2 Acceptance` 项目详情页。
2. 确认顶部工具栏、搜索框、语言列选择器、分页、表格都正常显示。
3. 翻到下一页或修改分页条件，如果数据不足，可以先批量创建一些临时词条。
4. 使用搜索框搜索 `home`。
5. 清空搜索框。
6. 使用语言列选择器只显示 `zh-CN`，再恢复显示全部目标语言。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 页面加载 | 无白屏、无报错弹窗 |
| 搜索框 | 输入后表格结果会更新 |
| 分页 | 搜索后分页总数和表格结果一致 |
| 语言列选择器 | 只影响显示列，不删除数据 |
| 表格 | Actions、Key、Remarks、截图列、基础语言列和目标语言列不错位 |

## 4. 必测项二：搜索结果 CSV 导出

原因：这是二阶段后追加的新需求。自动脚本验证了 CSV 格式，但真实浏览器下载、按钮禁用态、Excel/Numbers 打开效果需要人工确认。

### 4.1 按钮禁用态

步骤：

1. 清空搜索框。
2. 观察搜索框旁边的“导出搜索结果”按钮。
3. 输入不存在的搜索词，例如 `no-such-term-999`。
4. 再观察按钮状态。

期望结果：

| 场景 | 期望 |
| --- | --- |
| 无搜索词 | 按钮禁用 |
| 有搜索词但无结果 | 按钮禁用 |
| 有搜索词且有结果 | 按钮可点击 |

### 4.2 搜索范围

步骤：

1. 搜索 `Olá`，确认能搜到 `pt-BR` 命中的词条。
2. 用语言列选择器隐藏 `pt-BR`。
3. 保持搜索词 `Olá` 不变。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 搜索范围 | 仍能搜到 `pt-BR` 命中的词条 |
| 显示列 | 表格不显示已隐藏的 `pt-BR` 列 |
| 交互含义 | 隐藏列只影响显示，不影响项目已配置语言范围内的搜索 |

### 4.3 下载文件

步骤：

1. 搜索 `home`。
2. 点击“导出搜索结果”。
3. 用 Excel、Numbers 或常用表格工具打开下载的 CSV。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 导出范围 | 只包含搜索命中的全部行，不只当前页 |
| 列结构 | `Key`, `Remarks`, `en-US`, `zh-CN`, `pt-BR`, `ja-JP` |
| 截图 | CSV 不包含截图列 |
| 文件名 | 文件名包含项目名和搜索词 |
| 字符 | 中文、日文、`Olá`、重音字符、双引号、逗号不乱码、不串列 |

## 5. 必测项三：全量 CSV 导出和第一阶段 CSV 修复回归

原因：第一阶段改过 CSV BOM、`Remarks` 列和特殊字符处理；二阶段又扩展了搜索结果 CSV。需要人工用真实表格软件确认。

步骤：

1. 清空搜索框。
2. 点击项目工具栏中的全量 CSV 导出。
3. 用 Excel、Numbers 或常用表格工具打开 CSV。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 导出范围 | 包含项目全部词条 |
| 列结构 | `Key`, `Remarks`, 基础语言, 全部目标语言 |
| Remarks | 备注内容存在且列位置正确 |
| 特殊字符 | `ñ`, `ç`, `ã`, `¡`, `Olá`, 中文、日文不乱码 |
| escaping | 逗号、双引号、换行不会破坏列结构 |
| 截图 | 不包含截图相关字段 |

## 6. 必测项四：截图上传、预览、替换、删除和持久化

原因：截图功能涉及浏览器文件选择器、图片压缩、磁盘存储、预览弹窗和部署目录持久化，这些不能由自动脚本完整验证。

### 6.1 上传和预览

步骤：

1. 准备一张 PNG、JPG 或 WebP 图片，建议尺寸大于 1200 像素。
2. 在 `home.title` 行点击截图列图标。
3. 上传图片。
4. 再点击截图图标打开预览。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 上传 | 有成功提示 |
| 表格图标 | 显示有图状态 |
| 预览 | 在当前页面弹窗中显示图片 |
| 尺寸 | 图片不会撑破页面 |
| 压缩 | 大图上传后仍可快速预览 |

### 6.2 替换和删除

步骤：

1. 在预览弹窗中点击替换，上传另一张图片。
2. 再打开预览，确认显示新图片。
3. 点击删除并确认。
4. 刷新页面。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 替换 | 旧图被新图替换 |
| 删除 | 删除后图标回到未上传状态 |
| 刷新 | 刷新后不会显示旧图 |

### 6.3 部署持久化

如果使用 Docker 或服务器部署，需要确认 `data/` 是持久化目录。

步骤：

1. 在目标部署环境上传截图。
2. 重启应用或容器。
3. 再次打开同一词条截图。

期望结果：截图仍可预览。

如果重启后截图丢失，说明部署持久化配置没有完成。

## 7. 必测项五：基础语言列固定和语言列筛选

原因：第一阶段实现过基础语言 sticky，二阶段增加了截图列和语言列筛选，二者都影响表格横向布局。CSS sticky 错位需要真实浏览器确认。

步骤：

1. 保持多个目标语言列可见。
2. 横向滚动表格。
3. 点击基础语言列固定按钮。
4. 再次横向滚动。
5. 刷新页面。
6. 取消固定。
7. 隐藏部分目标语言列，再重复横向滚动。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 固定前 | 基础语言列正常随表格滚动 |
| 固定后 | 基础语言列横向滚动时保持可见 |
| 截图列 | 截图列位置稳定，不遮挡基础语言列 |
| 刷新后 | 固定状态在当前项目保留 |
| 取消固定 | 恢复正常滚动 |
| 语言列筛选后 | 表头和单元格仍然对齐 |

## 8. 必测项六：词条 CRUD 冒烟测试

原因：`0.3.26` 清理了词条创建、更新、清空后的返回查询字段。自动检查能保证类型，但浏览器保存、刷新和显示仍需要冒烟测试。

步骤：

1. 新建一个词条 `acceptance.crud.temp`。
2. 修改它的 Remarks 和一个目标语言内容。
3. 保存并刷新页面。
4. 执行清空目标语言操作。
5. 确认基础语言和 Remarks 保留。
6. 删除该词条并刷新页面。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 新建 | 成功出现在表格中 |
| 编辑 | 保存后刷新仍保留 |
| 清空 | 只清空目标语言，基础语言和 Remarks 保留 |
| 删除 | 删除后刷新不再出现 |
| UI 状态 | 没有保存后行数据丢失、图标异常或报错 |

## 9. 必测项七：文件导入 UI

原因：自动脚本验证了解析器，但没有通过浏览器文件上传控件和真实项目数据验证导入流程。

### 9.1 Android XML 导入

创建 `acceptance.xml`：

```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="import.android.title">Android title</string>
    <string name="home.title">Imported duplicate should go to remarks</string>
</resources>
```

步骤：

1. 在项目详情页点击导入。
2. 选择 `acceptance.xml`。
3. 查看导入结果和表格内容。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 新 Key | `import.android.title` 被新增 |
| 已有 Key | `home.title` 的基础语言不被覆盖 |
| 冲突值 | 冲突导入值被记录到 Remarks 或导入备注中 |
| 统计 | added / updated / skipped 统计合理 |

### 9.2 JSON 或 iOS strings 导入

任选一种再测一次真实上传。

H5 JSON 示例：

```json
{
  "import.h5.title": "H5 title",
  "import.h5.special": "Olá, ação and \"quotes\""
}
```

iOS strings 示例：

```text
"import.ios.title" = "iOS title";
"import.ios.line" = "Line 1\nLine 2";
```

期望结果：Key 被正确导入，特殊字符不乱码。

## 10. 必测项八：Pull API 真实 HTTP 调用

原因：格式契约已自动验证，但鉴权、真实路由、环境变量和部署反向代理需要人工或验收环境确认。

替换以下占位符：

| 占位符 | 含义 |
| --- | --- |
| `HOST` | 本地通常是 `http://localhost:3000` |
| `PROJECT_ID` | 测试项目 ID |
| `TOKEN` | `.env` 中的 `API_ACCESS_TOKEN` |

全量 JSON：

```bash
curl -H "Authorization: Bearer TOKEN" \
  "HOST/api/projects/PROJECT_ID/pull?format=json"
```

单语言 JSON：

```bash
curl -H "Authorization: Bearer TOKEN" \
  "HOST/api/projects/PROJECT_ID/pull?format=json&lang=zh-CN"
```

Android XML：

```bash
curl -H "Authorization: Bearer TOKEN" \
  "HOST/api/projects/PROJECT_ID/pull?format=xml&lang=zh-CN"
```

鉴权失败：

```bash
curl "HOST/api/projects/PROJECT_ID/pull?format=json"
```

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 正确 token | 返回 200 和翻译内容 |
| 无 token 或错误 token | 返回未授权错误 |
| 全量 JSON | 每个 Key 对应语言对象 |
| 单语言 JSON | 每个 Key 对应字符串，目标语言为空时 fallback 到基础语言 |
| XML | 返回 `<resources>` 和 `<string name="...">...</string>` |

## 11. 必测项九：重复内容检查 UI

原因：重复内容检查服务逻辑已实现，但结果弹窗、语言选择和真实 UI 交互需要人工确认。

步骤：

1. 确保 `button.save` 和 `button.save.copy` 的 `zh-CN` 都是 `保存`。
2. 打开重复内容检查功能。
3. 选择 `zh-CN`。
4. 运行检查。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 重复组 | 能看到 `保存` 这一组 |
| 词条列表 | 包含 `button.save` 和 `button.save.copy` |
| 空值 | 空翻译不计入重复 |
| 未配置语言 | 不能检查项目未启用语言 |

## 12. 必测项十：应用自身多语言浏览器体验

原因：i18n 是二阶段横向改动，涉及 cookie、浏览器语言和大量 UI 文案。自动检查不能确认真实语言切换体验。

步骤：

1. 切换 UI 到中文。
2. 刷新页面。
3. 打开项目列表、项目详情、项目设置、账号设置、截图弹窗、重复检查弹窗。
4. 切换 UI 到英文。
5. 再刷新页面并重复查看主要页面。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 语言保持 | 刷新后仍保持用户选择 |
| 主要文案 | 核心按钮、弹窗、toast、错误提示语言正确 |
| 项目语言 | UI 语言切换不改变项目基础语言和目标语言 |
| 可理解性 | 少量未翻译技术词不影响操作理解 |

## 13. 条件必测：AI 翻译

如果验收环境没有可用 AI 配置，可以跳过本节，并记录原因。

原因：AI 依赖外部服务、项目级配置、网络和模型输出，agent 无法稳定替代人工验收。

步骤：

1. 在项目设置里配置 AI Base URL、API Key、Model ID。
2. 找一个目标语言为空、基础语言有内容的词条。
3. 执行单格翻译。
4. 执行整列翻译或批量翻译。
5. 尝试在同一项目重复触发批量翻译。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 单格翻译 | 目标语言生成翻译 |
| 批量翻译 | 只翻译缺失项，不覆盖已有人工翻译 |
| 协议残留 | 结果中不出现 `|||`、锚点、Markdown 代码块等残留 |
| 并发保护 | 同项目重复触发批量翻译时应阻止并发 |
| 失败提示 | AI 失败时有可理解的错误提示或错误日志 |

## 14. 验收通过标准

`0.3.26` 可以认为通过人工验收，需要满足：

| 范围 | 标准 |
| --- | --- |
| 项目详情页 | 搜索、分页、语言列筛选、sticky 列不影响使用 |
| CSV | 全量导出和搜索结果导出在真实表格软件中可读 |
| 截图 | 上传、预览、替换、删除、部署持久化正常 |
| CRUD | 词条创建、编辑、清空、删除刷新后状态正确 |
| 导入 | 浏览器上传导入正常，冲突处理符合预期 |
| Pull API | 真实 HTTP 鉴权和 JSON/XML 输出正常 |
| 重复检查 | UI 能正确展示重复内容 |
| i18n | UI 语言切换和刷新保持正常 |
| AI | 如果配置可用，翻译结果可用且无协议残留 |

## 15. 问题记录模板

```text
问题标题：
测试环境：
当前分支：
测试账号：
项目名称：
操作步骤：
期望结果：
实际结果：
截图或导出文件：
是否可重复：
严重程度：
```

严重程度建议：

| 等级 | 含义 |
| --- | --- |
| P0 | 阻止登录、项目无法打开、数据丢失、构建失败 |
| P1 | 核心功能不可用，例如导入、导出、截图、搜索错误 |
| P2 | 有替代方案但影响验收，例如文案不清楚、局部样式错位 |
| P3 | 不影响使用的小问题 |
