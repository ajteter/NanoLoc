# NanoLoc 0.3.26 二阶段人工验收说明

## 1. 文档目的

这份文档用于验收 `0.3.26` 分支是否已经达到“可以交付给人类测试”的状态。

当前 `TODO.md` 里没有待实现功能，只有已实现记录。`0.3.26` 的目标不是继续新增核心功能，而是把二阶段已完成内容整理成一个可人工验收的版本。

验收人员可以按本文逐项操作，并在每一项后记录结果。

| 项目 | 结果 | 备注 |
| --- | --- | --- |
| 自动检查 | 未测试 / 通过 / 失败 |  |
| 登录和语言切换 | 未测试 / 通过 / 失败 |  |
| 项目创建和语言配置 | 未测试 / 通过 / 失败 |  |
| 词条 CRUD | 未测试 / 通过 / 失败 |  |
| 截图上传和预览 | 未测试 / 通过 / 失败 |  |
| 搜索和搜索结果导出 CSV | 未测试 / 通过 / 失败 |  |
| 全量 CSV 导出 | 未测试 / 通过 / 失败 |  |
| 文件导入 | 未测试 / 通过 / 失败 |  |
| Pull API | 未测试 / 通过 / 失败 |  |
| 重复内容检查 | 未测试 / 通过 / 失败 |  |
| AI 翻译 | 未测试 / 通过 / 失败 / 跳过 |  |
| 部署持久化 | 未测试 / 通过 / 失败 |  |

## 2. 验收前准备

### 2.1 确认分支

在项目目录执行：

```bash
git status --short --branch
```

期望看到当前分支是：

```text
0.3.26
```

### 2.2 准备环境变量

根目录需要 `.env` 文件。最少需要：

```env
DATABASE_URL="file:./dev.db?journal_mode=WAL&busy_timeout=5000"
AUTH_SECRET="replace-with-a-secret"
API_ACCESS_TOKEN="replace-with-a-test-token"
```

说明：

| 变量 | 用途 |
| --- | --- |
| `DATABASE_URL` | 本地 SQLite 数据库 |
| `AUTH_SECRET` | 登录认证需要 |
| `API_ACCESS_TOKEN` | Pull API 验收需要 |

如果要验收 AI 翻译，还需要在项目设置页配置 AI Base URL、API Key、Model ID 和 System Prompt。

### 2.3 初始化数据库并启动

执行：

```bash
npx prisma generate
npx prisma db push
npm run dev
```

打开：

```text
http://localhost:3000
```

如果没有账号，先注册一个测试账号，然后登录。

## 3. 自动检查

这些命令用于确认代码层面没有明显问题。建议在人工验收前先跑完。

```bash
npm run verify:contracts
./node_modules/.bin/tsc --noEmit
./node_modules/.bin/eslint .
./node_modules/.bin/next build --webpack
git diff --check
```

期望结果：

| 命令 | 期望 |
| --- | --- |
| `npm run verify:contracts` | 输出 `Storage format contracts verified.` |
| `tsc --noEmit` | 无错误 |
| `eslint .` | 无错误 |
| `next build --webpack` | 构建成功 |
| `git diff --check` | 无输出 |

`verify:contracts` 会检查导入解析和导出格式的基础契约，包括 Android XML、H5 JSON、iOS strings、CSV BOM、CSV escaping、Pull JSON、Pull XML 和基础语言 fallback。

## 4. 准备测试项目

建议新建一个干净项目，避免旧数据影响判断。

1. 进入项目列表页。
2. 点击创建项目。
3. 项目名填写：`Phase 2 Acceptance`
4. 基础语言选择：`en-US`
5. 目标语言选择：`zh-CN`、`pt-BR`、`ja-JP`
6. 保存项目。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 项目创建 | 成功进入项目详情页 |
| 基础语言 | 项目标题附近显示 `en-US` |
| 目标语言 | 表格中能看到 `zh-CN`、`pt-BR`、`ja-JP` |
| 页面报错 | 无报错弹窗，无空白页 |

## 5. 登录和应用自身多语言验收

### 5.1 语言切换

1. 找到页面上的语言切换入口。
2. 切换到中文。
3. 刷新页面。
4. 切换到英文。
5. 再刷新页面。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 中文显示 | 主要按钮、弹窗、表单、错误提示显示中文 |
| 英文显示 | 主要按钮、弹窗、表单、错误提示显示英文 |
| 刷新后保持 | 刷新后仍保持用户选择的语言 |
| 不影响项目语言 | UI 语言切换不改变项目的基础语言和目标语言 |

### 5.2 常见页面文案

检查这些页面是否还有明显未翻译或不影响理解的硬编码文案：

| 页面 | 检查内容 |
| --- | --- |
| 登录页 | 登录、注册、错误提示 |
| 项目列表 | 创建项目、空状态、搜索 |
| 项目详情 | 导入、导出、新建词条、搜索、分页 |
| 项目设置 | 基础语言、目标语言、AI 配置、删除项目 |
| 账号设置 | 修改名称、修改密码、提示信息 |

期望结果：主要业务操作都能被当前 UI 语言正确表达。

## 6. 词条 CRUD 验收

### 6.1 新建词条

在 `Phase 2 Acceptance` 项目详情页新建以下词条：

| Key | Remarks | en-US | zh-CN | pt-BR | ja-JP |
| --- | --- | --- | --- | --- | --- |
| `home.title` | `Home screen title` | `Hello, "world"` | `你好` | `Olá, ação` | `こんにちは` |
| `home.subtitle` | `Contains comma and newline` | `Line 1, welcome` | `第一行` | `Linha 1` | `行 1` |
| `button.save` | `CTA button` | `Save` | `保存` | `Salvar` | `保存` |
| `button.save.copy` | `Duplicate zh-CN value` | `Save copy` | `保存` | `Salvar cópia` | `保存コピー` |

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 新建成功 | 每个词条保存后出现在表格中 |
| Key 唯一 | 重复创建同一个 Key 时应提示冲突 |
| Remarks | 备注列能展示并保存 |
| 特殊字符 | 双引号、逗号、重音字符不乱码 |

### 6.2 编辑词条

1. 修改 `home.title` 的 Remarks 为：`Updated by acceptance test`
2. 修改 `pt-BR` 内容为：`Olá, mundo`
3. 保存。
4. 刷新页面。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 保存成功 | 页面提示保存成功 |
| 刷新保持 | 刷新后新内容仍然存在 |
| 审计信息 | 如果页面显示最后修改人，应显示当前测试用户 |

### 6.3 清空目标语言

1. 找到一个词条的更多操作。
2. 执行清空整行或清空目标语言操作。
3. 确认操作。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 基础语言 | `en-US` 内容保留 |
| Remarks | 备注保留 |
| 目标语言 | 目标语言内容被清空 |
| 页面状态 | 无报错，刷新后状态一致 |

### 6.4 删除词条

1. 新建一个临时词条 `temp.delete.me`。
2. 删除该词条。
3. 刷新页面。

期望结果：`temp.delete.me` 不再出现。

## 7. 截图功能验收

### 7.1 上传截图

准备一张 PNG、JPG 或 WebP 图片。建议图片大于 1200 像素，方便确认压缩。

1. 找到 `home.title` 行的截图列。
2. 点击截图图标。
3. 选择图片并上传。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 上传成功 | 页面显示成功提示 |
| 表格显示 | 截图列显示有图状态的图标 |
| 文件类型 | 上传 PNG/JPG/WebP 均可 |
| 文件大小限制 | 超过限制的图片应被拒绝并提示 |

### 7.2 预览截图

1. 点击已上传截图的图标。
2. 查看当前页面弹出的预览窗口。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 预览显示 | 能看到上传后的图片 |
| 图片尺寸 | 预览不撑破页面，能完整查看 |
| 页面位置 | 不跳转到新页面 |

说明：当前实现是点击图标在当前页面打开预览弹窗，不是新开页面。

### 7.3 替换截图

1. 在预览窗口点击替换。
2. 上传另一张图片。
3. 再次打开预览。

期望结果：预览显示新图片，不再显示旧图片。

### 7.4 删除截图

1. 在预览窗口点击删除。
2. 确认删除。
3. 刷新页面。

期望结果：截图图标回到未上传状态，预览入口不再显示旧图。

### 7.5 CSV 和 API 不包含截图

1. 导出全量 CSV。
2. 导出搜索结果 CSV。
3. 调用 Pull API。

期望结果：导出内容中没有截图路径、截图文件名、截图 MIME type 或截图大小字段。

## 8. 搜索和搜索结果 CSV 导出验收

### 8.1 无搜索条件

1. 清空搜索框。
2. 观察搜索框旁边的“导出搜索结果”按钮。

期望结果：按钮禁用。

### 8.2 有搜索条件但无结果

1. 输入一个不存在的词，例如：`no-such-term-999`
2. 观察表格和“导出搜索结果”按钮。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 表格 | 显示未找到词条 |
| 导出按钮 | 禁用 |

### 8.3 搜索 Key

1. 搜索：`home`
2. 查看结果。

期望结果：只显示 Key 包含 `home` 的词条。

### 8.4 搜索 Remarks

1. 搜索：`acceptance`
2. 查看结果。

期望结果：能搜索到 Remarks 包含 `acceptance` 的词条。

### 8.5 搜索目标语言内容

1. 搜索：`Olá`
2. 查看结果。

期望结果：能搜索到 `pt-BR` 内容包含 `Olá` 的词条。

### 8.6 隐藏语言列后继续搜索

1. 使用语言列选择器隐藏 `pt-BR` 列。
2. 搜索：`Olá`
3. 查看结果。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 搜索范围 | 仍能搜到 `pt-BR` 命中的词条 |
| 表格列 | 表格不显示已隐藏的 `pt-BR` 列 |
| 交互含义 | 隐藏列只影响显示，不影响项目配置语言范围内的搜索 |

### 8.7 导出搜索结果 CSV

1. 搜索：`home`
2. 点击“导出搜索结果”。
3. 打开下载的 CSV。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 导出范围 | 只包含搜索命中的行 |
| 分页范围 | 如果结果跨页，应导出全部命中行，不只当前页 |
| 列结构 | `Key`, `Remarks`, `en-US`, `zh-CN`, `pt-BR`, `ja-JP` |
| 截图 | 不包含截图列 |
| 文件名 | 文件名包含项目名和搜索词 |
| 字符 | `ñ`, `ç`, `ã`, `¡`, `Olá`, 中文、日文不乱码 |

## 9. 全量 CSV 导出验收

1. 清空搜索框。
2. 点击项目工具栏里的全量 CSV 导出。
3. 打开 CSV 文件。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 导出范围 | 包含项目全部词条 |
| 列结构 | `Key`, `Remarks`, 基础语言, 全部目标语言 |
| BOM | Excel 或 Numbers 打开后特殊字符不乱码 |
| escaping | 逗号、双引号、换行不会破坏列结构 |
| 截图 | 不包含截图相关列 |

可选技术检查：

```bash
xxd -l 3 downloaded.csv
```

期望首 3 个字节是：

```text
ef bb bf
```

## 10. 文件导入验收

建议分别测试 Android XML、H5 JSON、iOS strings。

### 10.1 Android XML

创建文件 `acceptance.xml`：

```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="import.android.title">Android title</string>
    <string name="home.title">Imported duplicate should go to remarks</string>
</resources>
```

操作：

1. 在项目详情页点击导入。
2. 选择 `acceptance.xml`。
3. 查看导入结果。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 新 Key | `import.android.title` 被新增 |
| 已有 Key | `home.title` 的基础语言不被覆盖 |
| 冲突值 | 被保留到 Remarks 或导入备注中 |
| 统计 | added / updated / skipped 统计合理 |

### 10.2 H5 JSON

创建文件 `acceptance.json`：

```json
{
  "import.h5.title": "H5 title",
  "import.h5.special": "Olá, ação and \"quotes\""
}
```

期望结果：两个 Key 被正确导入，特殊字符不乱码。

### 10.3 iOS strings

创建文件 `Localizable.strings`：

```text
"import.ios.title" = "iOS title";
"import.ios.line" = "Line 1\nLine 2";
```

期望结果：两个 Key 被正确导入，`\n` 被识别为换行内容。

## 11. Pull API 验收

Pull API 需要 `.env` 中存在 `API_ACCESS_TOKEN`。

先复制项目 ID。项目详情页 API 弹窗里可以看到项目 ID 和 curl 示例。

下面命令中的变量需要替换：

| 占位符 | 替换为 |
| --- | --- |
| `PROJECT_ID` | 当前测试项目 ID |
| `TOKEN` | `.env` 中的 `API_ACCESS_TOKEN` |
| `HOST` | 本地通常是 `http://localhost:3000` |

### 11.1 全量 JSON

```bash
curl -H "Authorization: Bearer TOKEN" \
  "HOST/api/projects/PROJECT_ID/pull?format=json"
```

期望结果：

| 检查点 | 期望 |
| --- | --- |
| HTTP 状态 | 200 |
| 格式 | JSON object |
| 内容 | 每个 Key 对应一个语言对象 |
| 语言范围 | 只包含项目配置的基础语言和目标语言 |

### 11.2 单语言 JSON

```bash
curl -H "Authorization: Bearer TOKEN" \
  "HOST/api/projects/PROJECT_ID/pull?format=json&lang=zh-CN"
```

期望结果：

| 检查点 | 期望 |
| --- | --- |
| HTTP 状态 | 200 |
| 格式 | JSON object |
| 内容 | 每个 Key 对应一个字符串 |
| fallback | 如果 `zh-CN` 为空，应使用 `en-US` |
| 空值 | 如果目标语言和基础语言都为空，可以不输出该 Key |

### 11.3 Android XML

```bash
curl -H "Authorization: Bearer TOKEN" \
  "HOST/api/projects/PROJECT_ID/pull?format=xml&lang=zh-CN"
```

期望结果：

| 检查点 | 期望 |
| --- | --- |
| HTTP 状态 | 200 |
| 格式 | XML |
| 根节点 | `<resources>` |
| 内容 | `<string name="...">...</string>` |
| escaping | `&`, `<`, `>`, `"` 等字符被正确转义 |

### 11.4 鉴权失败

1. 不带 Authorization header 调用 Pull API。
2. 使用错误 token 调用 Pull API。

期望结果：返回未授权错误，不返回翻译内容。

## 12. 重复内容检查验收

准备数据：

| Key | zh-CN |
| --- | --- |
| `button.save` | `保存` |
| `button.save.copy` | `保存` |

操作：

1. 打开重复内容检查功能。
2. 选择 `zh-CN`。
3. 运行检查。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 重复组 | 能看到 `保存` 这一组 |
| 词条列表 | 包含 `button.save` 和 `button.save.copy` |
| 空值 | 空翻译不计入重复 |
| trim | 前后空格不应造成误判 |
| 未配置语言 | 不能检查项目未启用的语言 |

如果有大小写忽略选项，可以再验证：

| 数据 | 期望 |
| --- | --- |
| `Save` 和 `save` | 开启忽略大小写后被识别为重复 |
| `Save` 和 `save` | 关闭忽略大小写后不一定被识别为重复 |

## 13. AI 翻译验收

如果当前环境没有可用 AI 配置，可以跳过本节，并在验收表里标记为“跳过”。

### 13.1 项目 AI 配置

1. 进入项目设置页。
2. 填写 AI Base URL、API Key、Model ID。
3. 保存设置。

期望结果：保存成功，刷新后配置仍存在。

### 13.2 单格翻译

1. 找到一个 `zh-CN` 为空、`en-US` 有内容的词条。
2. 点击单格翻译按钮。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 结果 | 目标语言生成翻译 |
| 协议残留 | 结果里不应出现 `|||`、锚点、Markdown 代码块等残留 |
| 错误提示 | AI 失败时有可理解的错误提示 |

### 13.3 整列或批量翻译

1. 选择一个目标语言。
2. 运行整列翻译或批量翻译。
3. 等待完成。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 缺失项 | 只翻译缺失内容 |
| 已有项 | 不覆盖已有人工翻译 |
| 并发 | 同一项目重复触发批量翻译时，应阻止并发执行 |
| 日志 | 失败项进入错误日志或显示错误提示 |

## 14. 基础语言列固定和语言列筛选验收

### 14.1 固定基础语言列

1. 项目中保留多个目标语言列。
2. 横向滚动表格。
3. 点击基础语言列的固定按钮。
4. 再次横向滚动。
5. 刷新页面。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 固定前 | 基础语言列正常随表格滚动 |
| 固定后 | 基础语言列横向滚动时保持可见 |
| 刷新后 | 固定状态保留 |
| 取消固定 | 再次点击后恢复正常滚动 |
| 项目隔离 | 另一个项目不受当前项目固定状态影响 |

### 14.2 语言列筛选

1. 通过语言列选择器只显示 `zh-CN`。
2. 确认 `pt-BR` 和 `ja-JP` 隐藏。
3. 刷新页面。
4. 清除筛选或重新显示所有语言。

期望结果：

| 检查点 | 期望 |
| --- | --- |
| 显示列 | 只显示选择的语言列 |
| 数据保存 | 隐藏列不会删除翻译内容 |
| 搜索范围 | 搜索仍在项目配置语言范围内工作 |
| 导出范围 | CSV 导出仍包含项目配置的全部目标语言 |

## 15. 部署和持久化验收

截图文件保存路径是：

```text
data/uploads/term-screenshots
```

`data/` 已被 gitignore，说明截图不会进入 Git 仓库。生产部署时必须保证 `data/` 是持久化目录。

### 15.1 本地持久化

1. 上传一张截图。
2. 停止开发服务。
3. 重新启动服务。
4. 打开同一个项目和词条。

期望结果：截图仍然可预览。

### 15.2 Docker 或服务器持久化

如果使用 Docker 部署，检查 `data/` 是否挂载到了宿主机或持久卷。

验收方式：

1. 部署应用。
2. 上传截图。
3. 重启容器。
4. 再次打开截图。

期望结果：截图仍然存在。

如果重启容器后截图丢失，说明部署持久化配置没有完成，不是前端操作问题。

## 16. 建议人工回归顺序

建议按以下顺序验收，能最快发现阻断问题：

1. 自动检查命令。
2. 登录和创建测试项目。
3. 新建、编辑、清空、删除词条。
4. 截图上传、替换、删除。
5. 搜索、语言列筛选、搜索结果 CSV 导出。
6. 全量 CSV 导出和文件导入。
7. Pull API JSON/XML。
8. 重复内容检查。
9. AI 翻译。
10. Docker 或服务器持久化。

## 17. 验收通过标准

可以认为 `0.3.26` 通过二阶段人工验收的条件：

| 条件 | 标准 |
| --- | --- |
| 自动检查 | 全部通过 |
| 核心 UI | 词条、搜索、导入、导出、截图、重复检查可正常操作 |
| 数据正确性 | 刷新后数据仍存在，CSV/Pull API 输出符合预期 |
| 特殊字符 | 中文、日文、葡语重音、逗号、双引号、换行不乱码、不串列 |
| 权限 | 未登录不能访问受保护 API，Pull API 需要 token |
| 持久化 | 数据库和截图在目标部署方式下不会因重启丢失 |
| 已知风险 | 如果跳过 AI 或 Docker 验收，需要明确记录原因 |

## 18. 问题记录模板

发现问题时建议按以下格式记录：

```text
问题标题：
测试环境：
当前分支：
测试账号：
项目名称：
操作步骤：
期望结果：
实际结果：
截图或导出文件：
是否可重复：
严重程度：
```

严重程度建议：

| 等级 | 含义 |
| --- | --- |
| P0 | 阻止登录、项目无法打开、数据丢失、构建失败 |
| P1 | 核心功能不可用，例如导入、导出、截图、搜索错误 |
| P2 | 有替代方案但影响验收，例如文案不清楚、局部样式错位 |
| P3 | 不影响使用的小问题 |
